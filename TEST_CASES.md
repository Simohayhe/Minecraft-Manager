# Beacon GUI テストケース

> チェックが入らなかった項目を Issue / 修正対象とする。
> 環境: Windows 10/11、Java 21 または 25 インストール済み

---

## 0. 初回起動・セットアップ

- [ ] アプリを初回起動するとセットアップ画面が表示される
- [ ] 「ここに作成する」でベースフォルダが作成され、メイン画面に遷移する
- [ ] 「既存のフォルダを選択」でフォルダを選んで設定できる
- [ ] 2回目以降の起動でセットアップ画面が出ず直接メイン画面が表示される

---

## 1. Java 管理（設定 → ☕ Java管理）

- [ ] Java管理タブが開ける
- [ ] 「自動検知」でシステム上のJavaが検出・一覧表示される
- [ ] Java 21 を Adoptium からインストールできる（ログが流れ、完了する）
- [ ] Java 25 を Adoptium からインストールできる
- [ ] インストール済みJavaの削除ができる
- [ ] exe を手動パス入力で追加できる
- [ ] 無効なパスを入力するとエラーになる

---

## 2. スタンドアロンサーバー

### 2-1. サーバー追加

- [ ] 「＋ サーバーを追加」ボタンが押せる
- [ ] Fabric / Paper の切り替えができる
- [ ] バージョン一覧が取得・表示される（26.1.x 含む）
- [ ] **Java 未インストール時に「インストール＆追加」を押すと警告が出てJava管理へ遷移する**
- [ ] サーバー名・ポートを入力して Fabric サーバーをインストールできる
- [ ] サーバー名・ポートを入力して Paper サーバーをインストールできる
- [ ] インストール失敗時に「インストールに失敗しました」と表示される（「完了」と出ない）
- [ ] インストール成功後にサーバーがリストに追加される

### 2-2. 起動・停止

- [ ] **Java 未設定時に起動ボタンを押すと警告モーダルが出てJava管理へ遷移する**
- [ ] 起動ボタンを押すとステータスが「起動中」になる
- [ ] サーバーが実際に起動してコンソールにログが流れる
- [ ] 停止ボタンでサーバーが停止してステータスが消える
- [ ] 再起動ボタンで停止→起動が行われる
- [ ] 起動中にアプリを閉じた後、再起動してもサーバーリストが正常に表示される
- [ ] 起動中にアプリを閉じた後、再起動してサーバーを再度起動できる（ゾンビプロセス残存しない）

### 2-3. コンソール

- [ ] コンソールタブでログがリアルタイム表示される
- [ ] コマンド入力欄からコマンドを送信できる（例: `list`）
- [ ] コマンドの結果がコンソールに表示される

### 2-4. サーバープロパティ

- [ ] プロパティタブで server.properties の内容が表示される
- [ ] 値を変更して保存すると実際にファイルに書き込まれる

### 2-5. 個別設定

- [ ] JVMメモリを変更して保存できる
- [ ] 特定の Java パスを指定できる
- [ ] 自動再起動の ON/OFF が切り替えられる
- [ ] ポート番号を変更できる（他のサーバーと重複するとエラー表示）

### 2-6. バックアップ・ワールド

- [ ] バックアップが実行され zip ファイルが作成される
- [ ] ワールドデータ削除が確認モーダル付きで実行できる

### 2-7. サーバー削除

- [ ] 削除確認モーダルが表示される
- [ ] 「ファイルも削除」チェックでフォルダごと削除される
- [ ] チェックなしで削除するとリストからだけ消えフォルダは残る

---

## 3. クラスターサーバー

### 3-1. クラスター作成

- [ ] 「＋ クラスターを追加」でクラスターが作成される
- [ ] クラスター内にサーバーを追加できる（Fabric / Paper）
- [ ] 追加した最初のサーバーが自動でメインサーバー（tryServers）に設定される

### 3-2. Velocity 設定

- [ ] Velocity タブが開ける
- [ ] 「Velocity を最新版に更新」でインストールが実行される（3.5.0-SNAPSHOT が取得される）
- [ ] インストール済みバージョンと最新バージョンが表示される
- [ ] 更新がある場合ボタンがオレンジ色になる
- [ ] ポート・転送モード・MOTDを変更して保存できる
- [ ] 転送シークレットのランダム生成が動作する
- [ ] バックエンドサーバー一覧が表示される

### 3-3. メインサーバー設定

- [ ] サーバーカードに「⭐ メインサーバー」チェックボックスが表示される
- [ ] チェックを入れると velocity.toml の `try = [...]` が更新される
- [ ] 複数サーバーをメインに設定できる
- [ ] サーバーを削除すると tryServers からも除外される
- [ ] **メインサーバーが0件の状態で Velocity を起動しようとすると警告が出る**

### 3-4. クラスター起動・停止

- [ ] Velocity 起動ボタンでプロキシが起動する
- [ ] 各バックエンドサーバーを個別に起動できる
- [ ] Velocity 停止ボタンでプロキシが停止する
- [ ] **起動中にアプリを閉じ、再起動後に再度起動できる**

### 3-5. クラスター削除

- [ ] クラスターとそのサーバーを削除できる
- [ ] 「ファイルも削除」でフォルダごと削除される

---

## 4. Mod / プラグイン管理

### 4-1. ライブラリ管理（Mod管理タブ）

- [ ] Mod管理タブが開ける
- [ ] フォルダ構造（Fabric版/Paper版）が表示される
- [ ] ファイルを追加できる（ファイル選択ダイアログ）
- [ ] ファイルを削除できる
- [ ] ライブラリの変更がリアルタイムで反映される（ウォッチ機能）

### 4-2. Modrinth 検索・ダウンロード

- [ ] Modrinth 検索が動作する
- [ ] 検索結果が表示される
- [ ] バージョンを選択してダウンロードできる
- [ ] ダウンロードしたファイルがライブラリに追加される

### 4-3. サーバーへの適用

- [ ] サーバーの Mod タブでライブラリ内の Mod が一覧表示される
- [ ] Mod を ON/OFF で切り替えるとサーバーの mods フォルダに反映される
- [ ] Fabric: Mod を有効にして起動したとき、サーバーに Mod が読み込まれる
- [ ] Paper: プラグインを有効にして起動したとき、サーバーに読み込まれる

### 4-4. 必須 Mod チェック（クラスター）

- [ ] クラスター内 Fabric サーバーの起動時に必須 Mod（ClusterConnect / FabricAPI）チェックが走る
- [ ] 不足している場合に警告モーダルが表示される
- [ ] 「修復して起動」で自動インストール→起動できる

---

## 5. 共有設定・Config 編集

- [ ] サーバーの Config タブが開ける
- [ ] Mod の設定ファイル一覧が表示される
- [ ] ファイルを開いて内容が表示される
- [ ] 編集して保存できる

---

## 6. パフォーマンス監視

- [ ] クラスターのパフォーマンスタブが表示される
- [ ] スタンドアロンのパフォーマンスタブが表示される
- [ ] 設定 → パフォーマンスタブで全サーバーの一覧が表示される
- [ ] 起動中サーバーの CPU・メモリがリアルタイム更新（約3秒）される
- [ ] 停止中サーバーは「停止中」表示になる
- [ ] ディスク容量が表示される（5分ごとに更新）

---

## 7. 設定タブ

### 7-1. DB 管理

- [ ] MariaDB のインストールができる
- [ ] DB の起動・停止ができる
- [ ] ステータスが正しく表示される

### 7-2. ポート管理

- [ ] 使用中ポート一覧が表示される
- [ ] ポートが重複しているサーバーに警告表示される
- [ ] UPnP ポート開放が実行できる

---

## 8. 診断タブ

- [ ] 診断タブが開ける
- [ ] グローバルIPが取得・表示される
- [ ] ローカルIPが表示される
- [ ] 外部からのポート疎通確認が実行できる
- [ ] インターネット速度測定が実行できる
- [ ] UPnP マッピング一覧が表示される

---

## 9. アプリ更新

- [ ] バージョンボタン（左下）が表示される
- [ ] クリックするとバージョンモーダルが開く
- [ ] 現在バージョンが正しく表示される
- [ ] GitHub からリリースノートが取得・表示される
- [ ] 更新がある場合にボタンが緑色になる
- [ ] 「今すぐ再起動して更新」で更新が適用される

---

## 10. エッジケース・異常系

- [ ] ベースフォルダが存在しない状態でアプリを起動した場合、セットアップ画面が表示される
- [ ] ポートが既に使用中の状態でサーバーを起動しようとした場合の挙動
- [ ] **サーバー起動中にアプリを強制終了し、再起動後にサーバーリストが正常表示される**
- [ ] **サーバー起動中にアプリを強制終了し、再起動後に同じポートで起動できる（ゾンビプロセスなし）**
- [ ] ネットワーク未接続の状態でバージョン一覧取得を試みた場合のエラーハンドリング
- [ ] インストール中にキャンセル（アプリ終了）した場合に中途半端なファイルが残らない
- [ ] 同じ名前のサーバーを2つ追加しようとした場合の挙動
- [ ] 同じポート番号のサーバーを2つ追加しようとした場合の挙動

---

## テスト環境メモ

| 項目 | 内容 |
|---|---|
| OS | |
| Javaバージョン | |
| MCバージョン | |
| テスト日 | |
| アプリバージョン | |
